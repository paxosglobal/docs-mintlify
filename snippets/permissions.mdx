export const Permissions = ({ value }) => (
  <Warning>
    <p><strong>Required Permissions</strong></p>
    <p>You need a role with the <code>{value}</code> permission to perform this action.</p>
  </Warning>
);

export default Permissions;