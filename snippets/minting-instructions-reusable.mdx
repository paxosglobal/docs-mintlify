export const MintingInstructionsReusable = () => (
  <Tip>
    <p><strong>Minting instructions are reusable!</strong></p>
    
    <img src="/images/saved-instructions.png" alt="Saved Instructions" />

    <p>Save the minting instructions to make them easy to retrieve from <a href="https://dashboard.paxos.com/dashboard/saved-instructions">Saved Instructions</a>. You can reuse the saved instructions any time you need to mint a stablecoin. Saved instructions are immutable, so once configured they always execute the exact same way.</p>
  </Tip>
);

export default MintingInstructionsReusable;