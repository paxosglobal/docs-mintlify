export const ButtonAdd = ({ label }) => (
  <button style={{ 
    background: "#007a57", 
    color: "#e5e5e5", 
    border: "none", 
    borderRadius: "4px",
    padding: "6px 12px",
    fontSize: "14px",
    fontWeight: "bold",
    cursor: "pointer"
  }}>
    <strong>{label}</strong>
  </button>
);

export default ButtonAdd;