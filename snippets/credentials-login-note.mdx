export const CredentialsLoginNote = () => (
  <Tip>
    <p>The Developer Sandbox is completely segregated from production, so credential configuration must take place in both environments.</p>
    <ul>
      <li><strong>Developer Sandbox</strong>: https://dashboard.sandbox.paxos.com/admin/api</li>
      <li><strong>Production</strong>: https://dashboard.paxos.com/admin/api</li>
    </ul>
  </Tip>
);

export default CredentialsLoginNote;
